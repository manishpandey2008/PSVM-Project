server:
  port: 8978

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedHeaders:
              - content-type
              - Authorization
            allowedMethods:
              - GET
              - POST
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/user/**
        - id: CALL-CENTER-SERVICE
          uri: lb://CALL-CENTER-SERVICE
          predicates:
            - Path=/api/center/**

#hystrix:
#  command:
#    fallbackcmd:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 4000

#management:
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream
